# 验收单 — eo-board --all 聚合页 v2：change 流首页 + 双视图切换 + 路由式下钻

> 验收基线：5e4e9c7（第 3 轮，2026-07-27 按验收反馈刷新；历史基线 3cf9553 → ff0e3a9）

## 验收反馈历史（AC-9）

**第 1 轮（基线 5c19342）** — 整体通过，两点要调整：

1. 「页面内容可以放到全宽，主要是下钻的视图，否则泳道展示不开」
2. 「整体的所有字号可以统一放大 0.5，现在字号有点小」

已在基线 ff0e3a9 落地：聚合首页与下钻泳道页的 `.wrap` 一并去掉宽度上限，两套模板加失效路由指引页共 106 处 `font-size` 每档 +0.5px。

**第 2 轮（基线 ff0e3a9）** — 全宽范围收窄：首页回 `max-width: 1280px` 居中，下钻泳道页保持全宽（依据：列表 / 流式视图定宽、看板 / 泳道视图全宽的行业惯例）。

已在基线 5e4e9c7 落地：`ALL_HTML_TEMPLATE` 的 `.wrap` 恢复 1280px 定宽居中（管首页 change 流与概要卡两视图），`PROJECT_CSS` 的 `.wrap` 维持全宽（管 serve 的 `/p/<key>` 与快照的 `#/p/<key>`）；`--html` 快照靠既有的样式表切换（进泳道视图时禁用 `aggStyle`、挂 `PROJECT_CSS`）天然分流，无需额外分支。字号档不动。**AC-9 因此重置为未勾，请按下方步骤重验。**

## 这次改了什么

`eo-board --all` 的一页看板首页从「每项目一张不可点的概要卡」换成了跨项目「进行中 change 流」——哪个 change 在动、卡在哪、进度多少一眼可见，3 天没动静的降权但不消失。顶部卡区多了个视图切换，可以切回原来的概要卡视图。项目条卡、change 行、概要卡现在都能点，直接进那个项目的泳道页，页头有「← 返回首页」。

## 环境准备

```bash
eo-board --all --serve        # 默认 7333 端口，自动开浏览器
```

（想看离线快照形态：`eo-board --all --html`）

## 逐项验收（人工项 1 条，验完勾「通过」即可）

### 1. 首页两视图的布局与密度过目（AC-9）

- 操作：
  1) 起 `eo-board --all --serve`，**把浏览器窗口拉到平时用的最大宽度**，看默认落地的 change 流视图——对照定稿 `eo-doc/changes/10-board-all-v2/design/variant-2.html`（浏览器直接打开这个文件比对）。定稿是 1180px 定宽、字号未加，现在是 1280px 定宽 + 每档 +0.5px，这两处差异是前两轮反馈要的，只对照结构、字段与密度分界
  2) 点顶部卡区的「概要卡」切到第二视图，对照改版前的概要卡感觉信息面有没有变薄
  3) 点任意一张项目条卡或一行 change，进泳道页看一眼——**六条泳道应能在当前窗口宽度内铺开**（不必再左右横滚），再点「← 返回首页」回来
  4) 顺手把窗口拖窄一次，确认窄屏下不出现横向溢出
  5) 想连快照一起验：`eo-board --all --html` 打开生成的单文件，确认 `#/` 与 `#/cards` 定宽居中、`#/p/<key>` 泳道全宽，与 serve 表现一致
- 预期：首页两视图内容居中、宽度封顶在 1280px（超宽屏两侧留白，行不会被拉成一条长线）；下钻泳道页吃满窗口宽度、左右留 24px 边距，六列一屏铺开；字号比第 1 轮明显舒服、无因放大导致的换行错位或元素挤压；change 流的条卡/行密度、降权分界与定稿一致或更好；概要卡视图不比改版前难用；下钻进去的是对的项目、返回回到 change 流视图
- [ ] 通过

## 已自动验证

其余 8 条已全部验证通过，见 [test.md](test.md)：AC-1/2/3/6/7/8 按批末轻验证 + eo-test 审计，AC-4（下钻路由 + 浏览器返回键历史恢复）与 AC-5（快照单文件 hash 路由、全程零网络请求）由 eo-test 首轮真实 Chrome 实测勾选。本轮 CSS 调整后全量 236 条单测复跑仍全绿（本轮只动 `.wrap` 宽度与 `font-size` 档，未触行为语义）。

---
对我说「带我验收」，我按本单逐项带你走：起环境、指路，你只逐项表态。
