# 验收单 — eo-helper 单一交互入口 + eo-board --all 聚合形态 + 命令面收纳

> 验收基线：b57a106（implement 每次生成/更新时刷新）

## 这次改了什么

终端侧现在只需记一条命令 `eo-helper`：数字菜单直达实时看板、多项目一页看板、注册、同步等全部高频动作，每次选择先回显底层命令再执行。多项目看板从终端表格升级为浏览器一页（`eo-board --all --html/--serve`，3 秒热刷新）。README 相应收纳成「一条命令 + 速查 + cli-reference 链接」。

## 环境准备

`sh install.sh`（或确认 `~/.local/bin` 里已有 eo-helper 链接）。无需其他数据或账号。

## 逐项验收（人工项 1 条，验完勾「通过」即可）

### 1. README 重构后新用户视角通读顺畅（AC-9）

- 操作：1) 打开仓库根 `README.md` 2) 以新用户视角通读「依赖」→「安装」→「第一次使用」→「看板与同步」四节 3) 检查是否只需记 `eo-helper` 一条命令即可跑通「初始化后看板 + 同步」日常
- 预期：叙述连贯不断链；「依赖」节只列真实运行时（无 smux 等过期条目）；「看板与同步」一节以 eo-helper 开场、速查不超过几条、深层参数指向 docs/cli-reference.md；无「投影」类内部术语
- [x] 通过（用户确认：「通过，直接归档」· 2026-07-26 · 基线 b57a106；首轮反馈「依赖部分可以删掉」已修 b57a106 后重验）

> 反馈轮记录（2026-07-26）：首轮通读反馈「依赖部分可以删掉了，现在已经不依赖 smux 这些了」——已按现状收缩「依赖」节（修复 commit b57a106，全仓 grep 佐证 smux 零消费），验收基线随之刷新，请按上方步骤重验。

## 已自动验证

其余 8 条（AC-1〜AC-8）已按批末轻验证通过：tests/ 全套 199 项测试绿（含聚合 html/serve 静态与调用计数断言、eo-helper 菜单/非 TTY/退出码矩阵、argparse 组合矩阵），另有临时前缀安装冒烟（AC-7）与无配置目录透传冒烟（AC-6）。本 change 尚未跑 eo-test（重验证与回归资产沉淀归 /eo-test）。

---
对我说「带我验收」，我按本单逐项带你走：起环境、指路，你只逐项表态。
