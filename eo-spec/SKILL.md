---
name: eo-spec
description: |
  模块需求规格技能。为一个模块撰写活文档 spec.md，作为该模块所有后续 change 的基线。

  USE FOR:
  - "写 spec" "需求分析" "模块规格" "spec 文档" "/eo-spec"
  - 任何为**新模块**定义需求基线的请求
  - 通常由 /eo-module-init 触发；独立调用时仍然只用于新模块
  - 对已有模块的修改请走 /eo-change（不再单独跑 spec）
---

# eo-spec — 模块需求规格（活文档）

将模块的模糊需求意图转化为清晰、无歧义的 **spec 活文档**，存放于 `eo-doc/dev/<module-name>/spec.md`。

## 定位（重要）

- **只用于新模块首次建立**：spec 是模块的"当前能力快照 + 持续演化基线"
- **不用于已有模块的修改**：已有模块的变更走 `/eo-change`，通过 Delta 增量演化 spec
- 通常由 `/eo-module-init` 作为其一个步骤调用；独立调用时也只处理新模块

## 核心原则

1. **不猜测**：遇到任何模糊点必须向用户提问澄清，绝不自行假设
2. **不模糊**：每个需求边界必须确定，In Scope / Out of Scope 必须明确列出
3. **不写代码**：绝不写任何代码，包括伪代码。只用自然语言描述需求行为
4. **活文档属性**：spec 不是一次性产物，会随后续 change 的 Delta 合并持续演化
5. **固定产出**：输出到 `eo-doc/dev/<module-name>/spec.md`

## 模板发现

启动时检查 `eo-doc/templates/`：
- 若 `project-profile.md` 存在 → 读取，了解项目类型和层级结构
- 若 `spec-layers.md` 存在 → 读取，启用"多层协作需求"章节
- 若上述文件不存在 → 使用纯单层模板，不生成多层章节

## 工作流程

### 第一步：理解与澄清

1. 阅读用户给出的需求描述
2. 阅读项目级背景文档（PRD、roadmap；如存在）
3. **执行模板发现**
4. **扫描 `eo-doc/dev/` 下已有的模块**（读取各 spec.md 的 frontmatter），避免重复或冲突
5. 列出所有不确定的点，逐一向用户澄清
6. 反复澄清直到无任何歧义

### 第二步：撰写 Spec

1. 确定 `module-name`（小写英文 kebab-case，如 `inventory` / `transport-queue`）
2. 创建目录 `eo-doc/dev/<module-name>/`（如不存在）
3. 按下方固定模板写入 `eo-doc/dev/<module-name>/spec.md`

### 第三步：用户确认

1. 将产出交给用户确认，根据反馈修订到满意
2. 用户确认后，将 frontmatter 的 `status` 从 `draft` 更新为 `confirmed`

### 第四步：更新索引

更新 `eo-doc/dev/INDEX.md`，添加模块条目。

## 固定模板

```markdown
---
title: <模块名称>需求规格说明
module_name: <module-name>
tags: [标签1, 标签2, 标签3]
created: YYYY-MM-DD
updated: YYYY-MM-DD
status: draft
summary: >
  一句话核心摘要，描述该模块的职责。
---

# <模块名称> 需求规格说明

> 状态：草稿 | 已确认（活文档，随 change 归档持续演化）
> 创建日期：YYYY-MM-DD
> 所属里程碑：如项目使用里程碑则填写，否则写"无"

## 1. 概述

### 1.1 模块简述
一句话说清楚这个模块做什么。

### 1.2 所属系统
标明属于哪个系统、子系统、包、程序集、服务或运行时区域。

### 1.3 关联文档
列出关联的 PRD 章节、roadmap、设计稿、相关模块 spec。

## 2. 用户故事与场景

### 2.1 目标用户
明确这个模块服务于谁。

### 2.2 用户故事
使用 "作为 <角色>，我希望 <行为>，以便 <价值>" 格式。

### 2.3 使用场景
描述 2-3 个典型使用场景，包含前置条件和期望结果。

## 3. 功能需求

### 3.1 输入
明确所有输入来源、格式、必填/选填。

### 3.2 输出
明确所有输出目标、格式、成功/失败的结果。

### 3.3 核心行为描述
用自然语言逐条描述模块在每个场景下的具体行为。
**禁止使用代码或伪代码。**

### 3.4 业务规则与约束
列出所有业务逻辑规则、限制条件、异常处理要求。

## 4. 非功能需求

### 4.1 性能要求
如有明确指标则列出，无则标注"无特殊要求"。

### 4.2 安全要求

### 4.3 兼容性要求

## 5. 边界与限制

### 5.1 明确包含（In Scope）
逐条列出本模块范围内的内容。

### 5.2 明确排除（Out of Scope）
逐条列出本模块明确不做的内容。

### 5.3 已知约束
列出技术或业务上的已知限制。

## 6. 验收标准（Acceptance Criteria）

使用 "Given-When-Then" 格式逐条列出：
- **Given** <前置条件>
- **When** <用户操作>
- **Then** <期望结果>

## 7. 多层协作需求（Multi-Layer Alignment）

> **条件章节**：仅在 `eo-doc/templates/spec-layers.md` 存在时生成。
> 章节结构由模板定义。若无模板，跳过此章节，后续章节编号顺延。

### 7.1 交互流向
描述本模块中各层之间的数据/控制流方向。
格式：`层A → 层B：做什么`，逐条列出。

### 7.2 各层职责划分

| 层 | 本模块中的职责 | 不做什么 |
|----|---------------|---------|

### 7.3 需要的数据与通信

| 数据/消息 | 提供方 | 消费方 | 格式要求 | 备注 |
|-----------|--------|--------|---------|------|

### 7.4 一致性检查清单

逐条列出各层之间必须保持一致的约束：
- [ ] 约束描述

> **禁止事项**：此章节只描述需求层面的协作关系。
> 不写接口签名、消息字段、代码结构。
> 不指定具体技术方案，只说明"需要什么"而非"怎么实现"。

## 8. 开放问题（Open Questions）

列出所有尚未解决的问题，标注负责人和期望解决日期。
如全部已解决则标注"无"。

## 9. 关联变更

> 此章节由 eo-archive 自动维护，归档的 change 会追加到此表。

| 变更 | 日期 | 摘要 |
|------|------|------|

## 10. 变更记录

| 日期 | 变更内容 | 变更人 |
|------|---------|--------|
| YYYY-MM-DD | 模块初始化 | eo-module-init |
```

## 关键约束

- **绝不写代码**：包括伪代码、代码片段、接口定义。这些属于 eo-change 阶段的职责
- **绝不猜测**：宁可多问一轮，也不要用"可能"、"大概"、"应该"这类词
- **一个 module 一个 spec**：不要在一个 spec 里塞多个不相关的模块
- **module-name 命名规则**：小写英文 kebab-case（如 `inventory` / `transport-queue`）
- **状态流转**：用户确认后必须更新 `status: draft` → `status: confirmed`
- **与 eo-change 的分工**：spec 只建一次基线；后续所有修改走 change 的 Delta
