# review.md 固定模板

eo-review 按下方模板维护 `eo-doc/changes/<change-id>/review.md`：**首轮全量写入，复审轮追加不覆盖**（只更新台账 + 追加轮次记录 + 原地更新速报）。

```markdown
---
title: <功能名称>代码审查报告
change_id: <change-id>
tags: [标签1, 标签2]
created: YYYY-MM-DD
updated: YYYY-MM-DD
status: active
summary: >
  一句话概述审查结论。
---

# <功能名称> 代码审查报告

> 关联 Change：[change.md](change.md)（检查表：其 §2 验收清单）
> 首轮审查日期：YYYY-MM-DD ｜ 审查范围：列出审查涉及的文件或模块
> ⚠️ 首轮之后正文各节为历史快照——当前状态以「Finding 台账」与末尾「速报」为准

## Finding 台账

<!-- 状态单一来源；轮次编号全文件单调递增（跨 revision 不清零）。写入权（writer matrix）：
     eo-review 建条与核销（open→verified；verified 后再打回 = reopen 回 open）；
     fixed + 修复 commit 由 eo-implement 修复循环回写；
     waived = 用户显式裁决不修（当场获得裁决的 skill 写入，附原话要点；不阻塞 reviewed/归档）；
     eo-change 回炉时追加作废行并把仍 open/fixed 的行批量标 superseded。历史轮次节谁都不改。
     根因枚举：implementation / requirement（打回实为需求问题 → 建议回炉） -->

| ID | 级别 | 摘要 | 位置 | 状态 | 根因 | 首见/最近轮 | 基线/修复 commit |
|----|------|------|------|------|------|-------------|------------------|
| P0-1 | P0 | <一句话> | `file:line` | open / fixed / verified / waived / superseded | implementation | 1/1 | `abc123` / ~ |

## 审查总结（首轮快照）

一段话概述代码整体质量，是否达到可合入标准。

## P0 - 必须修复（阻塞性问题）

### [P0-1] <问题标题>
- **类型**：Bug / 安全漏洞 / 逻辑错误 / 功能缺失
- **位置**：`文件路径:行号`
- **描述**：问题的具体描述
- **影响**：该问题会导致什么后果
- **建议**：修复方向（不写代码）

## P1 - 建议修复（重要但不阻塞）

### [P1-1] <问题标题>
- **类型**：架构问题 / 规范违反 / 潜在 Bug / 性能问题 / 注释纪律（溯源标注/叙事辩护）
- **位置**：`文件路径:行号`
- **描述** ｜ **建议**

## P2 - 可选优化（锦上添花）

### [P2-1] <问题标题>

## 验收标准覆盖检查

| AC 编号 | 描述 | 状态 |
|---------|------|------|
| AC-1 | ... | ✅ 通过 / ❌ 未通过 / ⚠️ 部分通过 |

## TODO 完成度检查

| TODO | 描述 | 状态 |
|------|------|------|
| TODO-1 | ... | ✅ 完成 / ❌ 未完成 / ⚠️ 部分完成 |

## 第 1 轮记录（revision R · YYYY-MM-DD）

- 审查基线：`<当前 plan_revision, H（最后一个业务代码或测试资产提交）>`
- 核销：无
- reopen：无
- 新增：无 ／ [P1-1] <一句话> — `file:line`（详情见上文）
- 测试证据处置：不适用 / 沿用 / 复验
- 既有通过 Test：无 / 第 N 轮 @ `<T>`；当前交付基线：`<H>`
- 受影响 AC / 测试：无 / <清单>
- 依据：<为何 T..H 不改变既有证据，或哪些证据已失效；一句话>
- 本轮结论：通过 / 不通过（P0 x）

<!-- 复审轮在「速报」之前按同一结构追加，不改既有轮次（finding 编号沿用同一序列，详情定点读对应轮次节）：

## 第 N 轮记录（revision R · YYYY-MM-DD）

- 审查基线：`<当前 plan_revision, H（最后一个业务代码或测试资产提交）>`
- 核销：P0-1 verified（修复 commit `def456`）；P1-1 open（未修到位：<一句话>）
- reopen：无 ／ P0-2 第二次打回（<一句话>）
- 新增：无 ／ [P1-3] <一句话> — `file:line`（详情本节展开）
- 测试证据处置：不适用 / 沿用 / 复验
- 既有通过 Test：无 / 第 N 轮 @ `<T>`；当前交付基线：`<H>`
- 受影响 AC / 测试：无 / <清单>
- 依据：<为何 T..H 不改变既有证据，或哪些证据已失效；一句话>
- 本轮结论：通过 / 不通过（P0 x）

回炉时由 eo-change 追加一行，历史轮次节保留不删：
> revision N 作废（YYYY-MM-DD）——后续见 revision N+1
-->

## 速报

结论：通过 / 不通过（P0 x）/ 有保留通过（P1 x）［第 N 轮 · revision R · 基线 `<short-sha>`］
⚠️ 复发：<ID> 第二次打回（无则省略此行）
测试证据处置：不适用 / 沿用 / 复验
既有通过 Test：无 / 第 N 轮 @ `<T>`；当前交付基线：`<H>`
受影响 AC / 测试：无 / <清单>；依据：<一句话>
下一步：回 /eo-implement 修复后复审 / 回 /eo-test 定向或完整复验 / 可进入 /eo-archive <change-id>
```

> 末尾「速报」节必填且恒为末节——它是本报告的机器可读出口（编排方与 eo-archive 读文件即取当前结论），与对话速报同款内容。每轮（含首轮）都保留结构化轮次记录，供后续 Test 精确引用触发 Review；速报中的「基线」= 本轮审查所覆盖的最后一个 `[<change-id>]` 业务代码或测试资产提交（eo-archive 归档门以此核对「交付变更后必须重新 review」）。`测试证据处置：沿用` 只在 reviewer 已审计完整 `T..H` 且沿用条件全部成立时可写；缺失、含糊或 `T` 不是 `H` 祖先均按 `复验`。
